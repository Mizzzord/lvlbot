# Настройка PostgreSQL для проекта LVLBot

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
pip install psycopg2-binary asyncpg
```

### 2. Настройка переменных окружения
Создайте или обновите файл `.env`:

```bash
# PostgreSQL настройки
POSTGRES_HOST=d165469ae0aa83744a530bc9.twc1.net
POSTGRES_DATABASE=default_db
POSTGRES_USER=gen_user
POSTGRES_PASSWORD=YOUR_REAL_PASSWORD_HERE  # Замените на настоящий пароль
POSTGRES_SSL_MODE=verify-full
POSTGRES_SSL_ROOT_CERT=ca.crt

# Включение PostgreSQL вместо SQLite
USE_POSTGRES=true
```

### 3. Тестирование подключения
```bash
# Установите переменные окружения
export POSTGRES_HOST="d165469ae0aa83744a530bc9.twc1.net"
export POSTGRES_DATABASE="default_db"
export POSTGRES_USER="gen_user"
export POSTGRES_PASSWORD="YOUR_REAL_PASSWORD"
export POSTGRES_SSL_MODE="verify-full"
export POSTGRES_SSL_ROOT_CERT="ca.crt"

# Протестируйте подключение
python3 postgres_config.py
```

### 4. Миграция данных (опционально)
Если у вас есть существующие данные в SQLite и вы хотите перенести их в PostgreSQL:

```bash
python3 test_postgres.py
```

## 📁 Структура файлов

- `postgres_config.py` - Конфигурация PostgreSQL
- `test_postgres.py` - Скрипт тестирования и миграции
- `database.py` - Адаптированная база данных (поддерживает SQLite и PostgreSQL)
- `ca.crt` - SSL сертификат для подключения

## 🔧 Переключение между базами данных

### Использование SQLite (по умолчанию):
```bash
USE_POSTGRES=false
```

### Использование PostgreSQL:
```bash
USE_POSTGRES=true
```

## 🛠 Технические детали

### Отличия PostgreSQL от SQLite:

1. **Типы данных:**
   - `INTEGER PRIMARY KEY AUTOINCREMENT` → `SERIAL PRIMARY KEY`
   - `BIGINT` для telegram_id
   - `TIMESTAMP DEFAULT CURRENT_TIMESTAMP`

2. **Синтаксис запросов:**
   - Параметры: `?` → `$1, $2, $3...`
   - `ON CONFLICT` поддерживается
   - Внешние ключи с `ON DELETE CASCADE`

3. **Подключение:**
   - SSL с сертификатом для безопасности
   - Асинхронные операции через `asyncpg`

### Архитектура:
- `Database` класс автоматически выбирает тип БД
- Все методы работают одинаково для обеих БД
- Миграция данных происходит автоматически при первом запуске

## 🚨 Важные замечания

1. **Пароль:** Замените `********` на настоящий пароль от базы данных
2. **Сертификат:** Файл `ca.crt` должен находиться в корневой директории проекта
3. **Безопасность:** Никогда не коммитьте реальные пароли в репозиторий
4. **Миграция:** Перед миграцией сделайте бэкап SQLite базы данных

## 🔍 Диагностика проблем

### Ошибка аутентификации:
```
FATAL: password authentication failed for user "gen_user"
```
**Решение:** Проверьте правильность пароля в переменной `POSTGRES_PASSWORD`

### Ошибка SSL:
```
certificate verify failed
```
**Решение:** Убедитесь, что файл `ca.crt` существует и доступен для чтения

### Ошибка подключения:
```
connection refused
```
**Решение:** Проверьте доступность сервера и правильность `POSTGRES_HOST`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Запустите `python3 postgres_config.py` для диагностики
3. Убедитесь, что все переменные окружения установлены корректно

# 🚀 Развертывание Motivation Bot с Docker

## 📋 Предварительные требования

- Docker >= 20.10
- Docker Compose >= 2.0
- **Аккаунт на Docker Hub** (для избежания rate limit)
- Минимум 2GB RAM
- Минимум 5GB свободного места на диске

## 🔑 Важно: Авторизация в Docker Hub

**Перед развертыванием выполните авторизацию:**
```bash
docker login
# Введите username и password от Docker Hub
```

Без авторизации вы можете столкнуться с rate limit ошибкой (100 pulls/6h для неавторизованных пользователей).

## 🔧 Быстрое развертывание

### 1. Клонирование и подготовка
```bash
git clone <your-repo-url>
cd lvlbot

# Копирование примера переменных окружения
cp env.example .env
```

### 1.5. Подготовка образов (опционально, на локальном компьютере)
```bash
# На компьютере с авторизацией в Docker Hub выполните:
./prepare-images.sh

# Перенесите созданные .tar файлы на сервер
# На сервере восстановите образы:
docker load < motivation-node.tar
docker load < motivation-python.tar
```

### 2. Настройка переменных окружения
Отредактируйте файл `.env` и заполните необходимые токены:

```bash
# Основной бот Telegram (получить у @BotFather)
BOT_TOKEN=your_main_bot_token_here

# Модераторский бот Telegram (отдельный токен)
MODERATOR_BOT_TOKEN=your_moderator_bot_token_here

# OpenRouter API (получить на openrouter.ai)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# WATA API для платежей (если используется)
WATA_TOKEN=your_wata_bearer_token_here
```

### 3. Проверка конфигурации (рекомендуется)
```bash
# Запуск проверки Docker конфигурации
./test-docker.sh
```

### 4. Запуск сервисов
```bash
# Сборка и запуск всех сервисов
docker-compose up -d --build

# Или с новым синтаксисом Docker Compose
docker compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка сервисов
docker-compose down
```

## 🏗️ Архитектура сервисов

### Сервисы:
- **card-generator**: Node.js сервис генерации карточек (порт 3000, Ubuntu-based)
- **bots**: Python контейнер с основным и модераторским ботами

### Базовые образы:
- **Node.js**: `node:18-bullseye-slim` (Ubuntu для совместимости с canvas)
- **Python**: `python:3.11-slim` (Debian для стабильности)

### Volumes:
- `bot-database`: База данных SQLite
- `player-cards`: Сгенерированные карточки игроков
- `player-photos`: Фото пользователей
- `task-submissions`: Отправленные задания

## 🔍 Проверка работы

### Проверка здоровья сервисов:
```bash
# Node.js сервис генерации карточек
curl http://localhost:3000/health

# Просмотр логов конкретного сервиса
docker-compose logs card-generator
docker-compose logs bots
```

### Проверка работы ботов:
1. Найдите ботов в Telegram
2. Отправьте `/start` основному боту
3. Проверьте генерацию карточек

## 🛠️ Управление сервисами

### Просмотр статуса:
```bash
docker-compose ps
```

### Перезапуск конкретного сервиса:
```bash
docker-compose restart card-generator
docker-compose restart bots
```

### Просмотр логов в реальном времени:
```bash
docker-compose logs -f bots
```

### Вход в контейнер для отладки:
```bash
docker-compose exec bots bash
docker-compose exec card-generator sh
```

## 🔧 Обновление

### Обновление образов:
```bash
# Остановка сервисов
docker-compose down

# Сборка с обновлением кэша
docker-compose build --no-cache

# Запуск обновленных сервисов
docker-compose up -d
```

### Обновление кода:
```bash
# Остановка сервисов
docker-compose down

# Обновление кода
git pull

# Перезапуск
docker-compose up -d --build
```

## 📊 Мониторинг

### Использование ресурсов:
```bash
docker stats
```

### Логи приложений:
- Основной бот: логи в контейнере `bots`
- Модераторский бот: логи в контейнере `bots`
- Node.js сервис: логи в контейнере `card-generator`

### Файлы логов на хосте:
- `moderator_bot.log` - логи модераторского бота
- Docker логи: `docker-compose logs > logs.txt`

## 🚨 Устранение неполадок

### Проблема: Docker Hub Rate Limit
```bash
ERROR: failed to build: failed to solve: node:18-bullseye-slim: failed to resolve source metadata for docker.io/library/node:18-bullseye-slim: failed to copy: httpReadSeeker: failed open: unexpected status code https://registry-1.docker.io/v2/library/node/manifests/sha256:...: 429 Too Many Requests - Server message: toomanyrequests: You have reached your unauthenticated pull rate limit.
```

**Решения:**
1. **Авторизуйтесь в Docker Hub:**
   ```bash
   docker login
   # Введите username и password
   ```

2. **Создайте аккаунт на Docker Hub** для увеличения лимита (200 pulls/6h вместо 100/6h)

3. **Используйте Docker Hub mirror:**
   ```bash
   # В /etc/docker/daemon.json добавьте:
   {
     "registry-mirrors": ["https://mirror.gcr.io"]
   }
   # Перезапустите Docker: sudo systemctl restart docker
   ```

4. **Используйте VPN или другой IP** для сброса лимита

5. **Подождите 6 часов** для автоматического сброса лимита

6. **Альтернатива: Используйте локальные образы**
   ```bash
   # Скачайте образы заранее на другом компьютере:
   docker pull node:18-bullseye-slim
   docker pull python:3.11-slim

   # Сохраните их:
   docker save node:18-bullseye-slim > node.tar
   docker save python:3.11-slim > python.tar

   # Загрузите на сервер и восстановите:
   docker load < node.tar
   docker load < python.tar
   ```

### Проблема: Ошибка сборки Node.js образа (canvas)
```bash
ERROR: failed to build: failed to solve: process "/bin/sh -c npm ci --only=production" did not complete successfully
```

**Решение:** Убедитесь, что используете обновленную версию `Dockerfile.nodejs` с Ubuntu базовым образом:
- Dockerfile изменен на `node:18-bullseye-slim` вместо Alpine
- Добавлены все необходимые системные зависимости для Linux
- Пересоберите образ: `docker-compose build --no-cache card-generator`

### Проблема: Боты не отвечают в Telegram
```bash
# Проверить токены в .env файле
docker-compose exec bots cat .env

# Перезапустить ботов
docker-compose restart bots
```

### Проблема: Ошибка генерации карточек
```bash
# Проверить Node.js сервис
docker-compose logs card-generator

# Проверить доступность сервиса
curl http://localhost:3000/health
```

### Проблема: База данных повреждена
```bash
# Остановить сервисы
docker-compose down

# Удалить старую БД (ВНИМАНИЕ: данные будут потеряны!)
rm bot_database.db

# Запустить сервисы (БД будет создана заново)
docker-compose up -d
```

## 🔒 Безопасность

### Рекомендации:
1. **Не коммитьте `.env` файл** в Git
2. **Используйте сильные токены** для ботов
3. **Регулярно обновляйте** образы Docker
4. **Ограничьте доступ** к серверу по SSH
5. **Настройте бэкапы** базы данных

### Переменные окружения:
- Хранятся в файле `.env`
- Не логируются в Docker образах
- Доступны только внутри контейнеров

## 📈 Масштабирование

### Для высокой нагрузки:
1. **Отдельные контейнеры** для каждого бота
2. **Redis/PostgreSQL** вместо SQLite
3. **Load balancer** для Node.js сервиса
4. **Мониторинг** с Prometheus/Grafana

### Пример с отдельными ботами:
```yaml
services:
  main-bot:
    build: .
    command: python bot.py
  moderator-bot:
    build: .
    command: python moderator_bot.py
  card-generator:
    # ...
```

## 🔄 Backup и восстановление

### Резервное копирование:
```bash
# Остановка сервисов
docker-compose down

# Копирование данных
tar -czf backup_$(date +%Y%m%d_%H%M%S).tar.gz \
  bot_database.db \
  player_cards/ \
  player_photos/ \
  task_submissions/

# Запуск сервисов
docker-compose up -d
```

### Восстановление:
```bash
# Остановка сервисов
docker-compose down

# Распаковка бэкапа
tar -xzf backup_file.tar.gz

# Запуск сервисов
docker-compose up -d
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs`
2. Проверьте статус сервисов: `docker-compose ps`
3. Проверьте использование ресурсов: `docker stats`
4. Создайте issue в репозитории с логами

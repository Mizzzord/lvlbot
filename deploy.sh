#!/bin/bash

# Скрипт быстрого развертывания Motivation Bot
# Использование: ./deploy.sh

set -e

echo "🚀 Начинаем развертывание Motivation Bot..."

# Проверка наличия Docker
if ! command -v docker &> /dev/null; then
    echo "❌ Docker не установлен. Установите Docker и попробуйте снова."
    exit 1
fi

# Проверка наличия docker-compose
if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose не установлен. Установите Docker Compose и попробуйте снова."
    exit 1
fi

# Проверка авторизации в Docker Hub
echo "🔐 Проверка авторизации в Docker Hub..."
if ! docker system info >/dev/null 2>&1; then
    echo "❌ Docker daemon не запущен или нет прав доступа."
    echo "Запустите: sudo systemctl start docker"
    exit 1
fi

# Проверка авторизации в Docker Hub
if ! docker info 2>/dev/null | grep -q "Username"; then
    echo "⚠️  Вы не авторизованы в Docker Hub. Это может вызвать rate limit ошибки."
    echo ""
    echo "Рекомендуется авторизоваться:"
    echo "1. Создайте аккаунт на https://hub.docker.com"
    echo "2. Выполните: docker login"
    echo "3. Введите username и password"
    echo ""
    read -p "Продолжить без авторизации? (y/N) " -n 1 -r
    echo
    if [[ ! $REPLY =~ ^[Yy]$ ]]; then
        echo "🔑 Выполните авторизацию и запустите скрипт снова:"
        echo "docker login"
        exit 1
    fi
fi

# Проверка наличия .env файла
if [ ! -f ".env" ]; then
    echo "📋 Создание .env файла из примера..."
    if [ -f "env.example" ]; then
        cp env.example .env
        echo "✅ Файл .env создан. Отредактируйте его перед запуском!"
        echo "🔧 Не забудьте заполнить BOT_TOKEN, MODERATOR_BOT_TOKEN и другие токены"
        read -p "Нажмите Enter после настройки .env файла..."
    else
        echo "❌ Файл env.example не найден!"
        exit 1
    fi
fi

# Проверка заполненности токенов
echo "🔍 Проверка переменных окружения..."
if ! grep -q "BOT_TOKEN=your_main_bot_token_here" .env; then
    echo "✅ BOT_TOKEN настроен"
else
    echo "⚠️  BOT_TOKEN не настроен в .env файле"
fi

if ! grep -q "MODERATOR_BOT_TOKEN=your_moderator_bot_token_here" .env; then
    echo "✅ MODERATOR_BOT_TOKEN настроен"
else
    echo "⚠️  MODERATOR_BOT_TOKEN не настроен в .env файле"
fi

# Остановка существующих контейнеров
echo "🛑 Остановка существующих контейнеров..."
docker-compose down 2>/dev/null || true

# Освобождение порта 3000
echo "🔌 Освобождение порта 3000..."
if lsof -Pi :3000 -sTCP:LISTEN -t >/dev/null 2>&1 ; then
    echo "Найден процесс на порту 3000, завершаем..."
    kill -9 $(lsof -ti:3000) 2>/dev/null || true
    sleep 2
else
    echo "Порт 3000 свободен"
fi

# Сборка и запуск сервисов
echo "🏗️  Сборка и запуск сервисов..."
echo "Это может занять несколько минут при первой сборке..."

# Попытка сборки с повторными попытками при rate limit
MAX_RETRIES=3
RETRY_COUNT=0

while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
    if docker-compose up -d --build; then
        echo "✅ Сервисы успешно запущены!"
        break
    else
        RETRY_COUNT=$((RETRY_COUNT + 1))
        if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
            echo "❌ Попытка $RETRY_COUNT/$MAX_RETRIES не удалась. Повтор через 30 секунд..."
            sleep 30
        else
            echo "❌ Все попытки сборки завершились неудачей."
            echo ""
            echo "🔧 Возможные решения:"
            echo "1. Авторизуйтесь в Docker Hub: docker login"
            echo "2. Подождите 6 часов для сброса rate limit"
            echo "3. Используйте VPN или другой IP"
            echo "4. Создайте аккаунт на Docker Hub для увеличения лимита"
            echo ""
            echo "📖 Подробная документация: DOCKER_DEPLOY.md"
            exit 1
        fi
    fi
done

# Ожидание запуска сервисов
echo "⏳ Ожидание запуска сервисов..."
sleep 10

# Проверка здоровья сервисов
echo "🏥 Проверка здоровья сервисов..."

# Проверка Node.js сервиса
if curl -f http://localhost:3000/health &>/dev/null; then
    echo "✅ Node.js сервис генерации карточек работает (порт 3000)"
else
    echo "❌ Node.js сервис не отвечает"
fi

# Проверка статуса контейнеров
echo ""
echo "📊 Статус контейнеров:"
docker-compose ps

echo ""
echo "🎉 Развертывание завершено!"
echo ""
echo "📋 Следующие шаги:"
echo "1. Проверьте работу ботов в Telegram"
echo "2. Отправьте /start основному боту"
echo "3. Проверьте генерацию карточек"
echo ""
echo "🔧 Управление:"
echo "• Просмотр логов: docker-compose logs -f"
echo "• Остановка: docker-compose down"
echo "• Перезапуск: docker-compose restart"
echo ""
echo "📖 Подробная документация: DOCKER_DEPLOY.md"
